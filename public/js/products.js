// نظام إدارة المنتجات
class ProductsManager {
    constructor(app) {
        this.app = app;
        this.products = [];
        this.categories = [];
        this.filters = {};
    }
    
    // دوال إدارة المنتجات
}

// إضافة للكائن العام
if (typeof window !== 'undefined') {
    window.ProductsManager = ProductsManager;
}
